packages:
  # Core utility macros and tests (unique combinations, relationships, etc.)
  - package: dbt-labs/dbt_utils
    version: [">=1.1.1", "<2.0.0"]

  # Optional but helpful: extended test and validation macros
  - package: calogica/dbt_expectations
    version: [">=0.10.3", "<1.0.0"]
